# Portable installer extras (USB / Windows cache). Never publish these.
ComfyUI/
.ssh/
.cursor/
.vscode/
AppData/

# Local agent notes (this machine only — not for GitHub)
/CURSOR.md
/HANDOFF.md

# update.sh moves copy-to-live tracked files here so origin can fast-forward
.tabby-update-backup/
# Written on the running install so git hooks can refuse commit/push
.live-install

# Editor and OS junk
.DS_Store
Thumbs.db
*.swp
*~
.idea/

# Accidental dumps in this tree
/new/
/harbor/
/spacediner/

# Generated ISO build artifacts
/.iso-work/
/out/

# Host-only ISO builder (Ubuntu + Docker). Same recipe as iso.yml.
/iso/build-docker.sh

# Runtime and secrets (also ignored inside tabbyAPI/)
**/venv/
tabbyAPI/ui/static/vs/
tabbyAPI/.lsp-tools/
**/models/
**/logs/
**/pasted-images/
**/.infinity_cache/
**/__pycache__/
**/*.pyc
**/*.log
**/config.yml
**/api_tokens.yml
ui_users.json
ui_sessions.json
**/tabby.env
/tabby-update-prompt.json
**/HOW-TO-ARCH.txt
**/saver-live.json
**/switch-model.log
**/ssh-forwarder.log
**/.env

# Keys and Cursor auth
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
*.pem
*.key
auth.json
